# Copy this file to .env and fill in your real values.
# Locally: use your local MySQL/MariaDB credentials.
# On Hostinger: use the MySQL database credentials from hPanel > Databases.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=petmax
DB_PORT=3306

# A long random string used to sign session cookies. Change this for production.
SESSION_SECRET=change-this-to-something-random

# Admin panel login password.
ADMIN_PASSWORD=petmax2026

PORT=3000
